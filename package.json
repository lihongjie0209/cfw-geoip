{
  "name": "cfw-geoip",
  "version": "1.0.0",
  "description": "Cloudflare Worker for IP geolocation using MaxMind GeoLite2 database from R2",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:auto": "node scripts/deploy.js",
    "tail": "wrangler tail",
    "test": "node scripts/test.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "wrangler": "^3.57.0"
  },
  "dependencies": {
    "buffer": "^6.0.3"
  }
}
